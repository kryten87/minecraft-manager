export enum EnvironmentVariables {
  PORTAINER_URL = 'PORTAINER_HOST',
  PORTAINER_USER = 'PORTAINER_USER',
  PORTAINER_PASSWORD = 'PORTAINER_PASSWORD',
}

export const Symbols = Object.freeze({
  Axios: Symbol('Axios'),
});
