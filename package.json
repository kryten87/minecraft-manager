{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn build:client && yarn build:server",
    "build:client": "/bin/bash ./scripts/build-client.bash",
    "build:server": "/bin/bash ./scripts/build-server.bash",
    "bump": "/bin/bash ./scripts/bump-version.bash"
  },
  "devDependencies": {
    "lerna": "^6.0.0"
  },
  "version": "0.5.1"
}
